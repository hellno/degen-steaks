type User {
  id: ID!
}

type Market {
  id: ID!
  creator: User!
  startTime: BigInt!
  targetPrice: BigInt!
  endTime: BigInt
  endPrice: BigInt
  totalHigher: BigInt!
  totalLower: BigInt!
  totalSteakedDegen: BigInt!
  totalDegen: BigInt
}

type Bet {
  id: ID!
  user: User!
  market: Market!
  amountHigher: BigInt!
  amountLower: BigInt!
}

type PlacedBet {
  id: ID!
  user: User!
  bet: Bet!
  market: Market!
  degen: BigInt!
  steaks: BigInt!
  feeSteaks: BigInt!
  betShares: BigInt!
  isHigher: Boolean!
}
